export class Status {
    private name: string;

    public set _name(name : string) {
        this.name = name;
    }
    
    private color: string;

    public set _color(color : string) {
        this.color = color;
    }
}
